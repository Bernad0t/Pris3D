---
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.8
build:
  requirementsPath: requirements.txt
run:
  persistenceMount: /data
  containerPort: 80
  command: uvicorn main:app --host 0.0.0.0
